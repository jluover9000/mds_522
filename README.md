`git branch <name>`: create a branch <name> in your current HEAD location
`git switch <name>`: switch to branch <name>
`git switch -c <name>`: create and switch to that branch <name>

i still dk what im doing...